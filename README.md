# X-Follow Assistant Pro (Chrome Extension)

## 1. 项目简介 | Introduction
**X-Follow Assistant Pro** 是一款基于 Chrome Manifest V3 标准开发的自动化关注工具，专为 Twitter (X.com) 深度运营设计。

它专注于在**帖子详情页 (Post/Status Page)** 自动扫描评论区用户，通过模拟真实人类的交互行为（悬停、等待卡片、点击）来执行关注操作。项目采用了 **Google 级工程标准**，包含完备的防封控启发式算法 (Heuristics)、双层日志系统以及鲁棒的错误边界处理。

---

## 2. 核心功能 | Core Features

### 💎 Freemium 商业模式 (New)
- **免费版 (Free Tier)**: 每月享有 **500 次** 自动化操作额度（关注 + 取关合计）。跨月自动重置。
- **专业版 (Pro Tier)**: 
  - **无限额度**: 解除所有操作限制。
  - **尊贵标识**: UI 界面显示金色 PRO 徽章。
  - **离线激活**: 支持通过激活码 (License Key) 离线解锁，保护隐私。

### 🛡️ 防封控机制 (Anti-Ban System)
- **拟人化抖动 (Humanizer Jitter)**: 关注间隔不再是固定值，而是在用户设定的区间（如 3-8秒）内遵循随机分布。
- **批次休息 (Batch & Rest)**: 支持设定“每关注 X 人，强制休息 Y 分钟”，打破机器人的连续操作特征。
- **微交互模拟**: 在点击前会有随机的微小停顿 (200-500ms)，模拟人类反应时间。

### 🧠 智能引擎 (Engine V2)
- **双模式扫描 (Hybrid Scanning)**:
  - **Direct Mode**: 针对点赞/转发列表（直接存在按钮）的场景。
  - **Hover Mode (新增)**: 针对评论区回复，自动执行 **"悬停 -> 等待资料卡 -> 扫描卡片 -> 点击关注"** 的复杂交互链条。
- **URL 守卫**: 自动检测当前页面是否为帖子详情页 (`/status/`)，防止在错误的页面运行。
- **蓝标过滤**: 可选仅关注认证用户 (Verified/Blue Tick)。

### 📊 监控与数据 (Analytics)
- **UI 仪表盘**: "暗夜极光" (Dark Aurora) 风格界面，实时显示统计数据及剩余额度。
- **持久化日志**: 最近 50 条操作日志自动保存到 `chrome.storage`，重启浏览器不丢失。
- **Google Analytics**: 集成 GA4 埋点，追踪安装量、活跃度及 Pro 激活转化率。

---

## 3. 技术架构 | Technical Architecture

项目采用原生 ES Modules (无构建工具依赖) 以确保极简与高性能。

```text
/
├── manifest.json            # V3 清单配置 (含 CSP 与 Background)
├── tools/                   # 开发者工具
│   ├── generate_keys.py     # 激活码生成器 (Python)
│   └── issued_keys.log      # 激活码发放记录
└── src/
    ├── popup/               # 用户界面
    │   ├── index.html       # 结构 (含 License Modal)
    │   ├── style.css        # 样式 (Glassmorphism)
    │   ├── main.js          # UI 逻辑与配置持久化
    │   └── license_utils.js # 离线激活校验逻辑
    │
    ├── content/             # 注入脚本 (核心逻辑)
    │   └── content.js       # 包含配额拦截器 (The Enforcer)
    │
    ├── background/          # 后台服务
    │   └── background.js    # 安装监听与 GA 事件转发
    │
    └── utils/
        └── analytics.js     # GA4 Measurement Protocol 封装
```

### 核心类设计 (`src/content/content.js`)
1.  **`Engine`**: 中央调度器。负责状态管理、批次计数、URL 校验以及 **配额检查**。
2.  **`DOMScanner`**: 负责解析 DOM。兼容 `[data-testid="tweet"]` (回复) 和 `[data-testid="UserCell"]` (列表) 两种结构。
3.  **`HoverCardController`**: **(核心)** 处理复杂的悬停交互。
4.  **`Humanizer`**: 提供 `randomDelay` 和 `tinyPause` 等工具函数。
5.  **`Logger`**: 双向日志系统 (Console + Runtime Message + Storage)。

---

## 4. 安装与使用 | Installation & Usage

### 安装步骤
1.  下载本项目源代码。
2.  打开 Chrome 浏览器，访问 `chrome://extensions/`。
3.  开启右上角的 **"开发者模式" (Developer mode)**。
4.  点击 **"加载已解压的扩展程序" (Load unpacked)**。
5.  选择项目根目录。

### 操作指南
1.  打开任意 X (Twitter) 帖子详情页 (例如: `https://x.com/username/status/12345...`)。
2.  点击浏览器右上角的 **X-Follow** 扩展图标。
3.  **检查额度**: 顶部状态栏会显示本月剩余免费额度。
4.  在面板中配置参数：
    - **Interval**: 建议设置 5-10 秒。
    - **Batch Size**: 建议 20-30 人。
    - **Rest Time**: 建议 5-10 分钟。
5.  点击 **"START ENGINE"**。

### Pro 版激活
1.  在插件面板点击顶部或底部的 **"升级 PRO"**。
2.  输入激活码 (格式: `XFP-xxxx-yyyy`)。
3.  点击激活，即刻解锁无限额度。

---

## 5. 开发者指南 (Developer Guide)

### 生成激活码 (License Generation)
本项目包含一个 Python 脚本用于生成离线激活码。
1.  确保已安装 Python 3。
2.  运行脚本：
    ```bash
    python3 tools/generate_keys.py
    ```
3.  按提示输入生成数量和备注。
4.  生成的激活码会自动追加保存到 `tools/issued_keys.log` 文件中。

> **⚠️ 安全警告**: 请务必修改 `tools/generate_keys.py` 和 `src/popup/license_utils.js` 中的 `SECRET_SALT` 为你自己的随机字符串，以防止激活码被破解。

### 数据埋点 (Google Analytics)
项目使用 Measurement Protocol 进行数据上报。
- **配置**: 修改 `src/utils/analytics.js` 中的 `GA_MEASUREMENT_ID` 和 `GA_API_SECRET`。
- **事件**: 
  - `app_install`: 插件安装/更新时触发。
  - `upgrade_click`: 用户点击升级按钮时触发。
  - `license_activated`: Pro 激活成功时触发。

---
*Updated: 2026-01-23 | Version: 1.0.2 (Pro Beta)*
